import { carFormInterface } from './car-form.interface';

describe('carDiscoverInterface', () => {
  it('should create an instance', () => {
    expect(new carFormInterface()).toBeTruthy();
  });
});
